# ===== frp 服务端配置（VPS 上运行）=====
# 使用：复制为 frps.toml，填入服务器 IP 与 auth.token（真实文件已被 .gitignore）
serverAddr = "YOUR_VPS_IP"
serverPort = 7000
auth.token = "YOUR_AUTH_TOKEN"

[[proxies]]
name = "streamlit"
type = "tcp"
localIP = "127.0.0.1"
localPort = 8501
remotePort = 8501
